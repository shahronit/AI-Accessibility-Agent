/** Product name shown in the UI, metadata, and exports. */
export const APP_NAME = "A11yAgent";

/** Short subtitle under the name in the shell. */
export const APP_TAGLINE = "Accessibility AI agent";

/** Default browser tab / SEO description. */
export const APP_DESCRIPTION =
  "A11yAgent scans pages with axe-core, explains findings with AI, supports voice control, and aligns reporting with WCAG.";

/** Full default document title. */
export const APP_TITLE_DEFAULT = `${APP_NAME} — ${APP_TAGLINE}`;
